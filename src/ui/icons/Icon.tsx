import { Icons as IconProps } from './icons'

type Props =
  | { name: 'fluentui'; color?: string }
  | { name: 'fluentui'; color?: string }

export const Icon = (props: IconProps) => {
  return <Icon></Icon>
}
